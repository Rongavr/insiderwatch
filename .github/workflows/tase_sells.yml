name: TASE Sell Alerts (2x daily)

on:
  workflow_dispatch:
  schedule:
    - cron: '45 7 * * 1-5'
    - cron: '35 15 * * 1-5'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow is valid"
